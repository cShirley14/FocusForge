# After deploying the SAM stack, fill these from the stack outputs:
#   aws cloudformation describe-stacks --stack-name focusforge --query "Stacks[0].Outputs"
VITE_API_URL=https://YOUR_API_ID.execute-api.us-east-1.amazonaws.com/dev
VITE_AWS_REGION=us-east-1
VITE_USER_POOL_CLIENT_ID=YOUR_CLIENT_ID
