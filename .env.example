# AWS Region for deployment
AWS_REGION=us-east-1

# Stack name (matches samconfig.toml)
STACK_NAME=focusforge

# Stage
STAGE=dev

# After first deploy, these are filled from `sam deploy` outputs:
# API_URL=
# USER_POOL_ID=
# USER_POOL_CLIENT_ID=
